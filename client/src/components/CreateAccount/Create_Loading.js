//if loading screen is comman to all projects than place it in utility component section
import React, { Component } from "react";

const styles = theme => ({

});

class CreateLoading extends Component {
    render() {
        return (
            <h1> Loading </h1>
        );
    }
}

export default CreateLoading
